# Commit Identity

If you want to make contributions and do not want your github profile shown on commits, configure this repo to use the SOREX committer identity:

```powershell
git config user.name "SOREX Committer"
git config user.email "sorex-committer@users.noreply.github.com"
```
